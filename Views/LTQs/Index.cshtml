@model IEnumerable<LeThuyQuynh123.Models.LTQ>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LTQId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LTQName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LTQGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LTQId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LTQName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LTQGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LTQId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LTQId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LTQId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
